# Delegate
Protocol-delegate pattern is a one-to-one communication between two objects, it could be a `UIViewController` and another `UIViewController`, a `UITableView` and it's controller, or between a custom class and whoever is using it.
