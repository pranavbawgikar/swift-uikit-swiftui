# SceneDelegate
Scene is a window of your app.

`SceneDelegate` handles window-level events.

### sceneDidDisconnect
`sceneDidDisconnect` fires after the scene enters the background or when it's session is discarded.

### sceneDidBecomeActive
`sceneDidBecomeActive` fires when the scene moves from an inactive state to an active state.
